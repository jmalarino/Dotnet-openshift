Sí, también puedes instalar `yq` en Red Hat Enterprise Linux 9.2 sin necesidad de utilizar Python. Una opción es descargar el ejecutable binario desde el repositorio oficial y luego añadirlo a tu ruta (PATH). Aquí te muestro cómo hacerlo:

1. **Descarga el ejecutable de `yq`**:
   Puedes descargar el ejecutable desde el repositorio oficial en GitHub. Por ejemplo, para la arquitectura x86_64, puedes utilizar el siguiente comando:
   ```bash
   sudo wget https://github.com/mikefarah/yq/releases/download/v4.13.4/yq_linux_amd64 -O /usr/local/bin/yq
   ```

2. **Asigna permisos de ejecución al archivo**:
   Una vez descargado, asigna los permisos de ejecución al archivo `yq`:
   ```bash
   sudo chmod +x /usr/local/bin/yq
   ```

3. **Verifica la instalación**:
   Puedes verificar si `yq` se instaló correctamente ejecutando:
   ```bash
   yq --version
   ```

   Esto debería mostrar la versión de `yq` instalada, lo que significa que la instalación fue exitosa.

Con estos pasos, `yq` debería estar instalado en tu sistema y listo para su uso.
